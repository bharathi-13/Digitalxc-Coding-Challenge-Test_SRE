[defaults]
inventory = ./inventory
remote_user = ubuntu
private_key_file = ../ssh-key/id_rsa
ask_pass = false@

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = false

[ssh_connection]
pipelining = True